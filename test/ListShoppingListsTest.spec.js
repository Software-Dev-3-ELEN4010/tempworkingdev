// var Vue = require('vue')
// import ListShoppingLists from '../public/src/components/ListShoppingLists.vue'

// describe('ListShoppingLists.vue', () => {
//   it(`should render propValue as its text content`, () => {
//     // Extend the component to get the constructor, which we can then initialize directly.
//     const Constructor = Vue.extend(ListShoppingLists);

//     const comp = new Constructor().$mount();

//     comp.shoppingListlistName = 'New Text';

//     Vue.nextTick(() => {
//       expect(comp.$el.textContent).to.equal('New Text');
//       done;
//     });
//   });
// });